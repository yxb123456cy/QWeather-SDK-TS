# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   verify:
#     name: verify (node ${{ matrix.node }})
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         node: [20, 22]
#     steps:
#       - uses: actions/checkout@v4

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9

#       - uses: actions/setup-node@v4
#         with:
#           node-version: ${{ matrix.node }}
#           cache: pnpm

#       - name: Install
#         run: pnpm install --frozen-lockfile

#       - name: Secret scan
#         run: pnpm secret:scan

#       - name: Typecheck
#         run: pnpm typecheck

#       - name: Lint
#         run: pnpm lint

#       - name: Test
#         run: pnpm test

#       - name: Build
#         run: pnpm build

#       - name: Package metadata (publint)
#         run: pnpm publint

#       - name: Type resolution (arethetypeswrong)
#         run: pnpm attw

#       - name: Size limit
#         run: pnpm size

#   release:
#     name: Release
#     runs-on: ubuntu-latest
#     needs: verify
#     if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#     permissions:
#       contents: write
#       pull-requests: write
#       id-token: write
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#           registry-url: https://registry.npmjs.org
#       - run: pnpm install --frozen-lockfile
#       - name: Create release PR or publish
#         run: pnpm release
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
